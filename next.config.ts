/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      'www.apple.com',
      'productimages.hepsiburada.net', // ← eklendi
    ],
  },
};

export default nextConfig;






